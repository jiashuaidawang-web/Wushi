-- ============================================================
-- 验证 rule_version 表结构 (001 已包含完整定义, 此处仅做部署后确认)
-- ============================================================
USE wushi;
SHOW COLUMNS FROM rule_version;
SHOW INDEX FROM rule_version;
